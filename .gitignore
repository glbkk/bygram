dist/build-stats.json
node_modules
.cache
.env

# ByGram ships serverless: the local Node server variant never belongs in this repo
server/
.env.server
dev/createBygramLanCertificate.sh
dev/runBygramLan.sh
dev/generateServerlessMusicCatalog.mjs
_legacy-server-variant/

# The music library ships as the committed build output in dist/bygram-music, so the source
# copy stays out of the repo. A build without it deletes that output, so restore
# public/bygram-music from dist before rebuilding.
public/bygram-music/

src/lib/gramjs/build/
build-contest/
tests/
.idea
*.iml
dev/perf/screenshot*
.DS_store
AGENTS.md
CLAUDE.md
.github/workflows/*
!.github/workflows/package-and-publish.yml
